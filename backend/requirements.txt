# ============================================
  # J.A.R.V.I.S Backend - LOCAL MODE REQUIREMENTS
  # No API keys needed! Everything runs locally.
  # ============================================

  # Core
  fastapi==0.115.0
  uvicorn==0.30.6
  websockets==13.0
  python-dotenv==1.0.1
  pydantic==2.9.2
  python-multipart==0.0.12

  # ============ LLM (Local Ollama) ============
  httpx==0.27.2

  # ============ STT (Local Whisper) ============
  faster-whisper==1.0.3
  sounddevice==0.5.1
  soundfile==0.12.1
  numpy==1.26.4

  # ============ TTS ============
edge-tts>=6.1.9
gTTS>=2.5.4
kokoro-onnx>=0.9.2
soundfile>=0.12.1
piper-tts>=2.0.0
  
  scipy==1.13.1
  pywin32==306; sys_platform == "win32"

  # ============ Memory (Local ChromaDB) ============
  chromadb==0.5.23
  sentence-transformers==3.1.1

  # ============ Screen/Vision (Local) ============
  mss==9.0.1
  pillow==10.4.0

  # ============ Computer Control ============
  pyautogui==0.9.54
  pygetwindow==0.0.9
  psutil==6.0.0

  # ============ Web Search (Free, No Key) ============
  duckduckgo-search==6.3.4
  beautifulsoup4==4.12.3

  # ============================================
  # OPTIONAL API KEYS
  # ============================================
  # ELEVENLABS_API_KEY=...    # Premium TTS (edge-tts is free and nearly as good)

  # ============================================
  # INSTALL:
  #   pip install -r requirements.txt
  # ============================================
  